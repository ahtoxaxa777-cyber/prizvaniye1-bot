# 🤖 БОТ "ПРИЗВАНИЕ" - ИНСТРУКЦИЯ ЗАПУСКА

## 📦 ЧТО ЭТО

Готовый Telegram бот для продажи курса по поиску призвания.

**Функции:**
- ✅ Выдача бесплатного Модуля 1
- ✅ 5 триггеров (добивок) через 1ч, 5ч, 24ч, 3д, 7д
- ✅ Оплата через ЮKassa (9,900₽)
- ✅ Автоматическая выдача модулей после оплаты
- ✅ База данных пользователей
- ✅ Сбор email перед оплатой

**Никаких ежемесячных платежей за ChatPlace!**

---

## 🚀 БЫСТРЫЙ СТАРТ (15 МИНУТ)

### ШАГ 1: Отключи бота в ChatPlace (1 мин)

1. Зайди в ChatPlace
2. Найди своего бота
3. Останови его (или просто не запускай)

**Важно:** Один токен = один активный бот. Нельзя запустить на двух платформах одновременно.

---

### ШАГ 2: Зарегистрируйся на Railway (2 мин)

1. Открой https://railway.app
2. Нажми "Login"
3. Войди через GitHub (если нет аккаунта GitHub - создай за 30 секунд)

**Бесплатно:** 500 часов/месяц = ~20 дней непрерывной работы

---

### ШАГ 3: Создай проект (5 мин)

1. В Railway нажми **"New Project"**
2. Выбери **"Empty Project"**
3. Нажми **"Deploy from GitHub repo"**

**НО!** Сначала нужно загрузить код на GitHub:

#### Вариант A: Через GitHub Desktop (проще)

1. Скачай GitHub Desktop: https://desktop.github.com
2. Установи
3. Войди в свой GitHub аккаунт
4. Нажми "Create New Repository"
5. Название: `prizvaniye-bot`
6. Выбери папку где сохранены файлы бота
7. Нажми "Publish repository"

#### Вариант B: Через командную строку (быстрее)

```bash
# 1. Перейди в папку с ботом
cd /путь/к/папке/с/ботом

# 2. Инициализируй git
git init

# 3. Добавь все файлы
git add .

# 4. Сделай коммит
git commit -m "Initial commit"

# 5. Создай репозиторий на GitHub (в браузере)
# github.com → New repository → prizvaniye-bot

# 6. Свяжи с GitHub
git remote add origin https://github.com/твой_username/prizvaniye-bot.git

# 7. Отправь код
git push -u origin main
```

---

### ШАГ 4: Подключи репозиторий к Railway (3 мин)

1. В Railway нажми **"Deploy from GitHub repo"**
2. Выбери репозиторий `prizvaniye-bot`
3. Railway автоматически обнаружит Python проект
4. Нажми **"Deploy"**

---

### ШАГ 5: Добавь переменные окружения (2 мин)

1. В Railway открой свой проект
2. Перейди на вкладку **"Variables"**
3. Добавь (если хочешь скрыть токены из кода):

```
BOT_TOKEN=8350801824:AAEOrk3Ffb_8ffwYlIjDnK6erPTvIUUsKfQ
YOKASSA_TOKEN=1268251:live_8qTPlKzYK16XLixZTYM1s2Dd46scDkO-h0HtYNJw4HM
```

**Это опционально!** Токены уже прописаны в `config.py`, но для безопасности лучше вынести в переменные.

Если добавил переменные - измени `config.py`:

```python
import os

BOT_TOKEN = os.getenv("BOT_TOKEN", "8350801824:AAEOrk3Ffb_8ffwYlIjDnK6erPTvIUUsKfQ")
YOKASSA_TOKEN = os.getenv("YOKASSA_TOKEN", "1268251:live_8qTPlKzYK16XLixZTYM1s2Dd46scDkO-h0HtYNJw4HM")
```

---

### ШАГ 6: Запусти бота (1 мин)

1. Railway автоматически деплоит бот
2. Дождись статуса **"Success"** (зелёная галочка)
3. Проверь логи: нажми на проект → вкладка "Deployments" → "View Logs"
4. Должно быть: `Бот запущен!`

---

### ШАГ 7: Протестируй (2 мин)

1. Открой Telegram
2. Найди своего бота (по username)
3. Напиши `/start`
4. Проверь:
   - ✅ Приветствие приходит
   - ✅ Кнопки работают
   - ✅ Модуль 1 выдаётся
   - ✅ Через 1 час (или 1 минуту если тестовый режим) приходит первый триггер

---

## ⚙️ НАСТРОЙКА БОТА

### Где что менять:

**1. Тексты бота → `texts.py`**
- Открой файл
- Найди нужный текст
- Измени
- Сохрани
- Запуши на GitHub:
  ```bash
  git add texts.py
  git commit -m "Обновил тексты"
  git push
  ```
- Railway автоматически перезапустит бот

**2. Ссылки (Notion, комьюнити) → `config.py`**
```python
NOTION_MODULE1 = "твоя_ссылка"
NOTION_MODULE2 = "твоя_ссылка"
# и т.д.

COMMUNITY_LINK = "https://t.me/твой_канал"
SUPPORT_USERNAME = "@твой_ник"
```

**3. Таймеры триггеров → `config.py`**
```python
# Для тестирования (быстрые триггеры)
TRIGGER_1_DELAY = 60      # 1 минута
TRIGGER_2_DELAY = 120     # 2 минуты
TRIGGER_3_DELAY = 180     # 3 минуты
TRIGGER_4_DELAY = 240     # 4 минуты
TRIGGER_5_DELAY = 300     # 5 минут

# Для продакшена (реальные интервалы)
TRIGGER_1_DELAY = 3600    # 1 час
TRIGGER_2_DELAY = 18000   # 5 часов
TRIGGER_3_DELAY = 86400   # 24 часа
TRIGGER_4_DELAY = 259200  # 3 дня
TRIGGER_5_DELAY = 604800  # 7 дней
```

**4. Цена курса → `config.py`**
```python
COURSE_PRICE = 9900  # в рублях
```

---

## 📊 КАК СМОТРЕТЬ СТАТИСТИКУ

### Вариант 1: Через Railway логи

1. Railway → твой проект → "Deployments" → "View Logs"
2. Там видно:
   - Кто запустил бота
   - Кто получил Модуль 1
   - Кто оплатил

### Вариант 2: Прямо в базе данных

База данных SQLite хранится в файле `bot.db` на Railway.

Чтобы посмотреть:

```bash
# Подключись к Railway через CLI
railway login
railway link

# Открой shell
railway shell

# Посмотри БД
sqlite3 bot.db

# Команды SQLite:
.tables                           # Показать таблицы
SELECT * FROM users;              # Все пользователи
SELECT * FROM tags;               # Все теги
SELECT COUNT(*) FROM users;       # Количество пользователей
SELECT COUNT(*) FROM tags WHERE tag='paid';  # Сколько купили
```

### Вариант 3: Админ-панель (опционально)

Если хочешь красивую админку - скажи, добавлю отдельный файл с веб-интерфейсом.

---

## 🔧 ЧАСТЫЕ ПРОБЛЕМЫ

### Бот не отвечает

**Решение:**
1. Проверь логи в Railway
2. Убедись что бот задеплоен (зелёная галочка)
3. Проверь что токен правильный
4. Отключил ли ChatPlace?

### Оплата не работает

**Решение:**
1. Проверь что ЮKassa токен правильный
2. Формат: `ShopID:SecretKey`
3. У тебя: `1268251:live_8qTPlKzYK16XLixZTYM1s2Dd46scDkO-h0HtYNJw4HM`

### Триггеры не приходят

**Решение:**
1. Проверь таймеры в `config.py`
2. Для теста поставь маленькие значения (60 секунд)
3. Убедись что пользователь не оплатил (триггеры останавливаются после оплаты)

### Railway пишет "Out of hours"

**Решение:**
Закончились бесплатные часы (500/месяц).

**Варианты:**
1. Подожди начала нового месяца
2. Оплати $5/месяц за безлимит
3. Перенеси на другой бесплатный хостинг (Render, Fly.io)

---

## 💰 СТОИМОСТЬ

**Railway:**
- Бесплатно: 500 часов/месяц (~20 дней работы)
- Платно: $5/месяц (безлимит)

**VS ChatPlace:**
- ChatPlace: 4,500₽/месяц (~$50)
- Railway: $5/месяц

**Экономия: ~$45/месяц = $540/год** 🔥

---

## 📝 СТРУКТУРА ФАЙЛОВ

```
bot/
├── bot.py              # Основной файл бота
├── config.py           # Настройки (токены, ссылки)
├── texts.py            # Все тексты бота
├── keyboards.py        # Кнопки
├── database.py         # База данных
├── triggers.py         # Система триггеров
├── requirements.txt    # Зависимости Python
└── README.md           # Эта инструкция
```

---

## 🎯 ЧТО ДАЛЬШЕ

1. **Протестируй бота** - пройди весь флоу от /start до оплаты
2. **Замени ссылки** - добавь все Notion модули в `config.py`
3. **Создай комьюнити** - канал/группу в Telegram
4. **Настрой таймеры** - сначала короткие для теста, потом реальные
5. **Запускайся!** 🚀

---

## ❓ ВОПРОСЫ

Если что-то не работает - пиши мне в бот, разберёмся!

Удачи с запуском! 💪
